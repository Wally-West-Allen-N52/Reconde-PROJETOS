@model IEnumerable<RVRentAgencia.Models.Contato>

@{
    ViewData["Title"] = "Index Contato";
}

<!DOCTYPE html>

<html>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>

<body>

    @* Corpo formulário *@
    @* Conteúdo de corpo *@
    <div class="container">
        <div class="row justify-content-md-center">
            <div class="col col-lg-2">
            </div>
            <div class="col-md-auto">
                <img src="~/img/RVRentHome.jpg" class="img-thumbnail" width="730x" height="380" alt="">
            </div>
            <div class="col col-lg-2">
            </div>
        </div>
        <div class="row justify-content-md-center">
            <button type="button" class="btn btn-success "><a asp-action="Create">Duvidas? Nos mande um Email</a></button>
        </div>
    </div>
    @* Conteúdo de corpo *@
    @* Corpo formulário *@
    
    <table class="table">
        @* <thead>
            <tr>
            <th>
            @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
            @Html.DisplayNameFor(model => model.Mensagem)
            </th>
            <th></th>
            </tr>
            </thead> *@
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Mensagem)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id_Contato">Editar</a> |
                        <a asp-action="Details" asp-route-id="@item.Id_Contato">Detalhes</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id_Contato">Deletar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>

</html>
